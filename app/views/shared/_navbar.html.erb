<nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="/"><%= image_tag("TM-logo.gif") %></a>
  <form class="form-inline">
    <% if current_page?(root_path) %> 
      <div>
        <%= link_to('Register Interest', new_interest_path, :class => 'btn btn-large btn-primary') %>
      </div>
    <% else %> 
      <div>
        <%= link_to('About Us', root_path, :class => 'btn btn-large btn-primary') %>
      </div>
    <% end %>
  </form>
</nav>
